interface:
  display_name: "TempoFastlane"
  short_description: "Proof-gated worker lanes with temporal calibration"
  default_prompt: "Use $tempofastlane to delegate bounded implementation work to the right worker lane with parent-side integration, causal proof, and TEMPONIZER wall-clock calibration."

policy:
  allow_implicit_invocation: true
