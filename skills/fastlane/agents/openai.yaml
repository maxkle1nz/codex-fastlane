interface:
  display_name: "Fastlane"
  short_description: "Proof-gated worker lanes with parent-side judgment"
  default_prompt: "Use $fastlane to delegate a bounded implementation to the right worker lane with baseline preflight, causal proof, and parent-side verification."

policy:
  allow_implicit_invocation: true
